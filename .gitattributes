# Normalize line endings for all text files to LF in the repo
* text=auto eol=lf

# Explicitly declare binary files so git never tries to diff / convert them
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
*.woff binary
*.woff2 binary
*.ttf binary
